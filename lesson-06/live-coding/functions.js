const age = 31;

const isRetired = age > 67;

// wenn rentner, dann eintrittspreis reduzieren
console.log(isRetired);

// Elemente zugreife, die im HTML sind
// finde den <button>... gibt es nicht, fehler
